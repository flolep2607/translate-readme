name: Translate all Markdown Action
author: Flolep2607
description: Translate all Markdown to another language

inputs:
  LANG:
    description: 'The Language you want to translate to'
    required: false
    default: 'en'

runs:
  using: 'node12'
  main: 'dist/index.js'

branding:
  icon: book-open
  color: green
